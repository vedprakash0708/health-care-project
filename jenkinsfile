pipeline {
    agent any
    
    environment {
        DOCKER_IMAGE = "vedantabharat/medicure-service:${BUILD_NUMBER}"
    }

    stages {
        stage('Checkout Code') {
            steps {
                git branch: 'main', url: 'https://github.com/vedprakash0708/health-care-project.git'
            }
        }
        
        stage('Build with Maven') {
            steps {
                sh 'mvn clean package -DskipTests' // Skip tests in package phase
            }
        }

        stage('Verify JAR File') {
            steps {
                sh 'ls -l target/'  // Debugging step to ensure JAR exists
            }
        }

        stage('Run Unit Tests') {
            steps {
                sh 'mvn test'
                junit 'target/surefire-reports/*.xml'
            }
        }

        stage('Login to DockerHub') {
            steps {
                withCredentials([usernamePassword(credentialsId: 'docker-hub-credentials', usernameVariable: 'DOCKER_USER', passwordVariable: 'DOCKER_PASS')]) {
                    sh 'echo "$DOCKER_PASS" | docker login -u "$DOCKER_USER" --password-stdin'
                }
            }
        }

        stage('Build Docker Image') {
            steps {
                sh "docker build -t ${DOCKER_IMAGE} ."
            }
        }

        stage('Push Docker Image') {
            steps {
                withCredentials([usernamePassword(credentialsId: 'docker-hub-credentials', usernameVariable: 'DOCKER_USER', passwordVariable: 'DOCKER_PASS')]) {
                    sh "docker push ${DOCKER_IMAGE}"
                }
            }
        }

        stage('Deploy to Kubernetes') {
            steps {
                script {
                    sh '''
                    echo "🔄 Checking Minikube status..."
                    minikube status || minikube start

                    echo "✅ Using Minikube context..."
                    kubectl config use-context minikube

                    echo "⏳ Waiting for Kubernetes nodes to be ready..."
                    kubectl wait --for=condition=Ready nodes --all --timeout=60s

                    echo "🚀 Deploying application..."
                    kubectl apply -f k8s-deployment.yaml --validate=false
                    kubectl rollout restart deployment medicure-deployment

                    echo "🔍 Checking pod status..."
                    kubectl get pods -o wide
                    '''
                }
            }
        }
    }

    post {
        always {
            archiveArtifacts artifacts: 'target/*.jar', fingerprint: true
            cleanWs()
        }
        success {
            echo "🎉 Deployment Successful!"
        }
        failure {
            echo "❌ Deployment Failed!"
        }
    }
}
